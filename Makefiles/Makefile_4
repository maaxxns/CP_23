#Now, we want to build into a different folder. We can either 
#change all 'build' strings in the previous file or we use 
#a variable
ODIR	= build_new

$(ODIR)/hello_world: hello_world.cpp | $(ODIR)
	g++ $< -o $@

$(ODIR):
	mkdir $(ODIR)
